#include <stdio.h>

/**
 * main - prints all alphabets in lowercase in reverse
 * Return: always 0.
 */

int main(void)
{
	char tebaphla;

	for (tebalpha = 'z'; tebaphla >= 'a'; tebaphla++)
		putchar(tebaphla);
	putchar('\n');
	return (0);
}
