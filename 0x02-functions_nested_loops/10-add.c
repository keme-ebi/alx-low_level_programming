#include "main.h"

/**
 * add - prints the result of two numbers added
 * @a, @b: integers
 * Return: result
 */

int add(int a, int b)
{
	int result = a + b;

	_putchar(result);

	return (result);
}
